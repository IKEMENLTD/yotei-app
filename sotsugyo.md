# 設計書: 教室予定の管理アプリ

作成日: 2026-09-04 / 改訂: 2026-09-07
状態: レビュー反映済み。着手前に「12. 未決事項」の確認が必要

## 1. 背景

教室の予定を紙の台帳で管理しており、予定の書き直しが頻繁に発生している。
書き直しの手間をなくすことを、今回の題材として選んだ。

他に「月末の集計を手で数えている」「講師への連絡がバラバラで伝え漏れる」という困りごとがあるが、今回は対象外とする。

## 2. MVPの一文

このアプリは、**教室の予定を管理する担当者**が、**予定の登録・書き換え・取りやめを画面から行うこと**で、**紙の台帳を書き直さずに最新の予定を確認できる**ようになるアプリです。

## 3. 前提

未確認のため、以下を仮の前提として設計している。変われば設計も変わる。

| # | 前提 | 変わった場合の影響 |
| --- | --- | --- |
| 1 | **使うPCが担当者専用である**（共用ではない） | 共用なら誰でも全予定を閲覧・改ざんできる。最優先の確認事項（12章） |
| 2 | 使う人は1人。PCのブラウザで使う | 複数人・スマホ対応なら画面と保存方法が変わる |
| 3 | データは手元のPC内に保存する | 共有が必要ならサーバとデータベース、および**ログインが同時に**必要になる |
| 4 | 一覧は日付順のリストで足りる | カレンダー表示が必要なら画面が大きく変わる |
| 5 | 予定1件を上書き変更できればよい。変更履歴は残さない | 振替の紐づけが必要ならデータ構造が変わる（12章で実測して判断） |
| 6 | 1日10件前後、数ヶ月分 | 規模が大きく変わる場合は絞り込みと性能を再検討 |
| 7 | **講師名は扱う。生徒の情報と講師の連絡先は扱わない** | 生徒情報を持つなら保存先とアクセス制限を再検討 |

前提1と3は他と性質が違い、崩れると設計そのものが変わる。着手前に確認する。

## 4. スコープ

| 今回作る機能 | 今回は作らない機能 |
| --- | --- |
| 予定の登録（日付・時間・教室・講師） | 月末の集計・レポート出力 |
| 予定の一覧表示（既定は今日以降、日付順） | 講師への連絡・通知の送信 |
| 予定の変更・削除（削除は取り消せる） | ログイン・複数ユーザーの権限管理 |
| 休講の記録（消さずに残す） | 紙の台帳の過去データの取り込み |
| 既存の予定を複製して登録 | 繰り返し予定の自動生成 |
| 教室・講師の選択式入力とマスタ管理 | 変更履歴の保持 |
| 入力チェックとエラー表示 | 振替の紐づけ（12章の実測後に判断） |
| 予定の重なりの警告（保存は止めない） | |
| **全データのファイル書き出し・読み込み** | |

書き出し機能は当初「未決」だったが、4方向のレビューがいずれも同じ結論に達したため作る機能に格上げした。理由は、消える可能性が残る限り担当者は紙にも書き続け、1章の困りごとが解決しないため。

## 5. データ設計

3つのまとまりで持つ。教室と講師をマスタに分けるのは表記ゆれ（「A教室」「Ａ教室」）を防ぐため。

### 5.1 予定 `schedules`

| 列 | 型 | 必須 | 内容・制約 |
| --- | --- | --- | --- |
| `id` | 文字列(UUID) | ○ | 主キー。生成後は変更しない |
| `date` | 文字列 `YYYY-MM-DD` | ○ | 開催日。ゼロ埋め固定長 |
| `start_time` | 文字列 `HH:MM` | ○ | 24時間表記のゼロ埋め |
| `end_time` | 文字列 `HH:MM` | ○ | `start_time` より後。開始+既定時間を自動入力 |
| `room_id` | 文字列(UUID) | ○ | 教室への参照 |
| `instructor_id` | 文字列(UUID) | ○ | 講師への参照 |
| `status` | 文字列 | ○ | `scheduled` / `canceled`。既定 `scheduled` |
| `note` | 文字列 | − | 業務上の補足のみ |
| `deleted_at` | 文字列(ISO日時) or null | − | 論理削除。null なら生きている |
| `created_at` | 文字列(ISO日時) | ○ | |
| `updated_at` | 文字列(ISO日時) | ○ | 保存のたび更新 |

- 並び順: `date` → `start_time` の昇順。どちらもゼロ埋め文字列なので辞書順がそのまま時系列順になる
- 一意制約は付けない。「同じ教室・同じ時間」は警告するが保存は許す方針のため
- 休講は `status`、削除は `deleted_at` と役割を分ける。休講は記録として残すもの、削除は入力ミスの取り消し

### 5.2 教室 `rooms`

| 列 | 型 | 必須 | 内容 |
| --- | --- | --- | --- |
| `id` | 文字列(UUID) | ○ | 主キー |
| `name` | 文字列 | ○ | 一意。前後の空白を除いて保存（例: A教室） |
| `sort_order` | 整数 | ○ | 選択肢の並び順 |
| `is_active` | 真偽 | ○ | false で新規選択肢から外す。過去の予定は残る |

### 5.3 講師 `instructors`

| 列 | 型 | 必須 | 内容 |
| --- | --- | --- | --- |
| `id` | 文字列(UUID) | ○ | 主キー |
| `name` | 文字列 | ○ | 一意（例: 山田太郎） |
| `sort_order` | 整数 | ○ | |
| `is_active` | 真偽 | ○ | 契約終了などで false |

連絡先は持たない。「講師への連絡」はスコープ外であり、持てば個人情報の範囲が広がるため。

### 5.4 検証ルール

| 対象 | ルール | 違反時 |
| --- | --- | --- |
| 日付・開始・終了・教室・講師 | 空でない | 保存しない。該当欄の下に理由を表示 |
| `end_time` | `start_time` より後 | 保存しない |
| 文字列全般 | 前後の空白を除去してから空判定 | − |
| 教室の重なり | 同一教室・同一日で `開始 < 相手の終了` かつ `終了 > 相手の開始` | 警告のみ。保存は許す |
| 講師の重なり | 同上を講師で判定 | 警告のみ |

## 6. 画面

1. **一覧画面**（起点）
   - **起動時は今日以降を表示**。日付の区切り見出しを入れる
   - 期間で絞り込む。**現在の絞り込み条件と「解除」を常時表示する**
   - 「まだ1件も登録していない」と「絞り込み結果が0件」を別の文言で出し分ける
   - 各行に「編集」「複製して登録」「削除」
   - 休講は取り消し線などで残す
   - 「予定を追加」ボタン、「ファイルに書き出す／読み込む」ボタン
2. **登録・変更フォーム**
   - 5.1の項目を入力。教室と講師は選択式
   - 違反は保存せず、該当欄の下にまとめて全部表示する
   - 入力途中で離れるときは確認する
   - メモ欄に「業務上の補足のみ。個人の事情は書かない」と注記を出す
3. **削除の確認**
   - 確認後、一覧に「削除しました／元に戻す」を数秒表示する

初回起動でデータが空のときは、「まだ予定がありません。『予定を追加』から始めてください」と案内する。教室と講師が未登録だと予定を登録できないため、**先に教室・講師を登録する導線**を用意する。

## 7. 操作の流れ

- 予定を追加する: 一覧 →「予定を追加」→ 入力 → 保存 → 一覧に反映
- 似た予定を追加する: 一覧 → 対象の「複製して登録」→ 日付だけ変更 → 保存
- 予定を書き換える: 一覧 → 対象の「編集」→ 修正 → 保存 → 一覧が更新される
- 休講にする: 一覧 → 対象の「編集」→ 状態を休講に → 保存 → 一覧に取り消し線で残る
- 入力を間違えた: 一覧 → 対象の「削除」→ 確認 → 一覧から消える（直後なら元に戻せる）

紙の台帳での「消して書き直す」作業が、3番目と4番目の流れに置き換わる。

## 8. 技術構成と実装方針

- ブラウザだけで動く1ページのアプリ（HTML / CSS / JavaScript）
- データはブラウザ内に保存する。サーバやデータベースは使わない

ビルド環境を導入しても利点が乏しく、環境構築で時間を失う risk のほうが大きいため、この構成で確定とする。

### 8.1 着手前に決めておく5点

手が止まりやすい箇所なので先に固定する。

1. **保存形式**: 全データを1つのキーに、下記のJSONを丸ごと入れる。数百件なら毎回全件を書き換えて問題ない
2. **ID**: `crypto.randomUUID()` を使う。連番は削除後に採番が重複し、「たまに別の予定が消える」という追いにくいバグになる
3. **日付と時刻**: 分けて文字列で持つ。`Date` 型やISO日時で持つとタイムゾーンのずれとパースの差で必ず詰まる
4. **描画**: データ配列を唯一の正とし、変更のたびに一覧を全部描き直す。差分更新は状態同期のバグに時間を取られる
5. **画面切替**: 3つの領域の表示・非表示のみ。削除確認はブラウザ標準の確認ダイアログで足りる

### 8.2 実装規約

- 入力値の表示は `textContent` を使い、`innerHTML` に渡さない

### 8.3 保存するJSONの形

```json
{
  "version": 1,
  "rooms": [
    { "id": "r-01", "name": "A教室", "sort_order": 1, "is_active": true }
  ],
  "instructors": [
    { "id": "t-01", "name": "山田太郎", "sort_order": 1, "is_active": true }
  ],
  "schedules": [
    {
      "id": "s-01",
      "date": "2026-09-10",
      "start_time": "10:00",
      "end_time": "11:30",
      "room_id": "r-01",
      "instructor_id": "t-01",
      "status": "scheduled",
      "note": "",
      "deleted_at": null,
      "created_at": "2026-09-07T11:00:00",
      "updated_at": "2026-09-07T11:00:00"
    }
  ]
}
```

`version` を先頭に置くことで、書き出したファイルを読み込むときに形式の違いを判定でき、後から列を足しても壊れない。**この構造をそのまま書き出せばバックアップファイルになる。**

### 8.4 この構成の弱点

- 同じPCの同じブラウザからしか見られない
- ブラウザの保存データを消すと予定も消える → 8.3の書き出しで補う

## 9. 運用

- **紙からの切り替え**: 決めた日以降の予定はアプリのみで管理し、それ以前は紙を参照用に残す。移行時は先の1〜2週間分だけ手入力する。過去データの取り込みは行わない
- **バックアップ**: 週1回ファイルに書き出し、業務PCの決まったフォルダに置く。個人のUSBやメール添付で持ち出さない
- **並行運用**: 切り替え日以降は紙に書かない。二重管理になると書き直しの手間はむしろ増える
- **個人情報**: 講師名を扱う。メモ欄に健康状態などの私的事情を書かない

## 10. 受け入れ条件（完成の判定）

すべて満たせば完成とする。

- [ ] 予定を登録すると、一覧の該当日付の位置に1件増えている
- [ ] ブラウザを再読み込みしても、登録した予定が残っている
- [ ] 予定を編集して保存すると、一覧の表示が新しい内容に変わっている
- [ ] 休講にした予定が、消えずに休講と分かる形で一覧に残っている
- [ ] 削除した予定が一覧から消え、直後なら元に戻せる
- [ ] 日付を空にして保存しようとすると、保存されず理由が表示される
- [ ] 終了時間を開始時間より前にすると、保存されず理由が表示される
- [ ] 同じ教室・同じ時間帯に登録しようとすると警告が出て、それでも保存できる
- [ ] 起動直後の一覧に今日以降が表示され、過去の予定を探さずに済む
- [ ] データが0件のとき、案内が表示される
- [ ] ファイルに書き出し、データを消してから読み込むと、元の状態に戻る

発表用に、この流れを3分でたどるデモ手順を別途用意する。

## 11. 将来サーバ構成へ移行する場合のチェックリスト

**今回のスコープ外。** 現構成では鍵もデータベースも存在しないため、以下はいずれも現時点で該当しない。共有が必要になった時点で、上から順に実施する。

### 11.1 鍵の扱い

- [ ] git を使い始めるなら、最初のコミットより前に `.env` を除外設定に入れる
- [ ] 鍵はソースに直書きせず環境変数から読む。`.env.example` に値を入れない
- [ ] リポジトリを公開にするか非公開にするかを最初に決める
- [ ] ブラウザに配る鍵は露出前提。**その鍵が危険になるのはアクセス制御が無い場合**で、鍵の秘匿ではなく11.2で守る
- [ ] 管理者用の鍵・DBパスワード・接続文字列はサーバ側のみに置く。これらはアクセス制御を迂回する
- [ ] ビルド時にJSへ埋め込まれる変数の接頭辞規則を確認する
- [ ] 鍵が漏れた場合に作り直す手順を決めておく
- [ ] 画面収録・スクリーンショットに開発者ツールや設定ファイルを映さない

### 11.2 行単位のアクセス制御（RLS）

順番に意味がある。飛ばすと下の項目が無意味になる。

- [ ] **1. 認証を先に入れる。** 行の所有者を判定できなければアクセス制御は成立しない
- [ ] **2. 各テーブルに所有者・所属を表す列を足す。** 5章のスキーマには未搭載
- [ ] **3. アクセス制御を有効化する。** 多くのDBで既定は無効＝全行が誰にでも見える状態
- [ ] **4. 教室と講師のテーブルも対象に含める。** 講師の一覧は個人名の名簿そのもの
- [ ] **5. 暫定で置いた「全員に許可」の設定を消す。** 有効化直後はアプリが動かなくなるため暫定設定を書きがちで、そのまま忘れるのが最も多い抜け
- [ ] **6. 参照・追加・更新・削除のそれぞれに設定する**
- [ ] **7. 更新時は書き込み側の条件も設定する。** 読み取り条件だけだと自分の行を他人のものに書き換えられる
- [ ] **8. 未ログイン状態に何が許されているか明示的に確認する**
- [ ] **9. 論理削除をアプリ側のフィルタだけで隠さない。** API経由で削除済みの行が読める
- [ ] **10. 別アカウントで実際に試す。** 設定を書いたことと効いていることは別

## 12. 未決事項

着手前に確認する。上から優先度順。

- [ ] **使うPCは担当者専用か、共用か**（前提1）。共用ならアプリにログインを足すのではなく、OSのユーザーまたはブラウザのプロファイルを分ける運用で担保する
- [ ] **直近1ヶ月の書き直し10件の内訳を数える**（振替／休講／時間変更／その他）。振替が過半なら `schedules` に元の予定への参照列を追加する
- [ ] **講師はこの予定をどう見るのか。** 紙の台帳を見に来ていた・掲示していたなら、画面だけでは講師側が不便になる。印刷が必要かを確認し、4章のどちらかの列に入れる
- [ ] 台帳にあって5章に入っていない項目はないか
- [ ] 作業PCに開発ツールを入れられるか（8章の構成なら不要だが、確認しておく）
- [ ] 前提2〜7が実態と合っているか
